[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"

# Always fetch the service worker fresh so updates reach your phone
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
